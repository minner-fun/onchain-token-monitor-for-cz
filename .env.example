# check.py needs nothing here — it runs against a public BSC RPC out of the box.
# These are for the full 24/7 monitor (Telegram alerts on the 4 signals).

# Optional custom BSC RPC endpoint (default: https://bsc-dataseed.binance.org)
BSC_RPC=

# Optional Telegram alerts
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
